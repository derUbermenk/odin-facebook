<div class="user-preview">
  <%= link_to user_path(user) do %>
    <div class="name">
      <strong> <%= user.username %> </strong>
    </div>
  <% end %>
  <div class="mutual-friend-count">
    <%= pluralize(user.mutual_friends(current_user).count, 'mutual friends') %>
  </div>
</div>